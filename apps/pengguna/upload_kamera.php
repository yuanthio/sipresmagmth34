<!-- <?php
if (isset($_FILES['image'])) {
    $target_dir = "../../apps/pengguna/kamera/";
    $file_name = basename($_FILES["image"]["name"]);
    $target_file = $target_dir . $file_name;

    move_uploaded_file($_FILES["image"]["tmp_name"], $target_file);
}
?> -->
